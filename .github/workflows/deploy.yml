name: Deploy to Azure

on:
  pull_request:
    types: [ "labeled" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Setup Node.js environment
        uses: actions/setup-node@v3.5.0
        with:
          node-version: 16

      - name: GitHub Script
        uses: actions/github-script@v6.3.2
        with:
          script: |
            //console.log(JSON.stringify(context, null, 2));
            console.log(JSON.stringify(github.rest, null, 2));
            if (context.payload.label.name === 'deploy') {
              console.log('Deploying to Azure...')
            } else {
              console.log('Not deploying to Azure...')
            }

            console.log(JSON.stringify(github, null, 2));
            console.log(JSON.stringify(env, null, 2));
            console.log(JSON.stringify(job, null, 2));
            console.log(JSON.stringify(jobs, null, 2));
            console.log(JSON.stringify(steps, null, 2));
            console.log(JSON.stringify(runner, null, 2));
            console.log(JSON.stringify(secrets, null, 2));
            console.log(JSON.stringify(strategy, null, 2));
            console.log(JSON.stringify(matrix, null, 2));
            console.log(JSON.stringify(needs, null, 2));
            console.log(JSON.stringify(inputs, null, 2));
