name: Deploy to Azure

on:
  pull_request:
    types: [ "labeled" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: GitHub Script
        uses: actions/github-script@v6.3.2
        with:
          script: |
            console.table(${{github}});
            /*console.dir(JSON.stringify(${{github.event}}));
            console.dir(JSON.stringify(${{github.event.pull_request}}));
            console.dir(JSON.stringify(${{github.event.pull_request.labels}}));
            console.dir(JSON.stringify(${{github.event.pull_request.labels[0]}}));
            console.dir(JSON.stringify(${{github.event.pull_request.labels[0].name}}));
            console.dir(JSON.stringify(${{github.event.pull_request.labels[0].name}}) === "deploy");
            if (JSON.stringify(github.event.pull_request.labels[0].name) === "deploy") {
              console.dir("Deploying to Azure");
            } else {
              console.dir("Not deploying to Azure");
            }*/
